.idea
bin/
obj/
.env
.env.json
# The bot's per-account Discord statuses, written beside the code by `dotnet run`.
.status.json
*.user
Services/Gaida.API/cache/

# The Spotify pod and scripts/ are Python; running either leaves these behind.
__pycache__/

# pytest, from running the pods' own tests outside their image.
.pytest_cache/

# Bind-mounted volumes: the library, the covers and every cache compose.yaml points at ./data.
data/
